name: FlaskProject Application Testing

defaults:
  run:
    working-directory: flaskproject

on:
  push:
    branches: [master]
    # Commenting out line below for better CI debugging
    #paths:
    #  - 'flaskproject/**'
  pull_request:
    branches: [master]
    # Commenting out line below for better CI debugging
    #paths:
    #  - 'flaskproject/**'
  # schedule:
  #  - cron: '30 * * * *'

jobs:
  semgrep:
    name: Semgrep Scan
    runs-on: ubuntu-latest
    permissions:
      actions: read 
      contents: read
      security-events: write
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      #- name: Semgrep Github CI
      #  uses: returntocorp/semgrep-action@v1
      #  with:
      #    config: >- # more at semgrep.dev/explore
      #      p/security-audit
      #      p/secrets
        
      - name: Install Semgrep
        run: python3 -m pip install semgrep

      - name: Run Semgrep
        run: semgrep --config p/security-audit . -o sastall/res/flaskproject-semgrep.sarif --sarif

      # Rofl guess we'll just not upload this
      #- name: Upload SARIF file
      #  uses: github/codeql-action/upload-sarif@v1
      #  with:
      #    sarif_file: /home/runner/work/SASTAll/SASTAll/flaskproject-semgrep.sarif #IMPORTANT: for some reason checkout_path doesn't work??
      #    #checkout_path: /home/runner/work/SASTAll/SASTAll/flaskproject # THIS IS SO BAD SHOULD NOT BE MANUALLY DEFINING THIS BUT NO DOCUMENTATION AaAaa

      - name: Upload Semgrep Results as artifact
        uses: actions/upload-artifact@v2
        with:
          name: semgrep-${{ hashFiles('flaskproject-semgrep.sarif') }}
          path: sastall/res/
  codeql:
    name: CodeQL Analysis - Test application
    runs-on: ubuntu-latest
    permissions:
      actions: read
      contents: read
      security-events: write
    strategy:
      fail-fast: false
      matrix:
        language: [ 'python' ]
    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Initialize CodeQL
        uses: github/codeql-action/init@v1
        with:
          languages: ${{ matrix.language }}

      - name: Autobuild
        uses: github/codeql-action/autobuild@v1

      - name: Perform CodeQL Analysis
        uses: github/codeql-action/analyze@v1

      - name: (REMOVE THIS LATER) debugging
        run: ls
      
      # TODO: get a way to find a local version of the CodeQL 

      #- name: Upload SARIF file
      #  uses: github/codeql-action/upload-sarif@v1
      #  with:
      #    sarif_file: ./results/python.sarif # this is what i found to be the file its being uploaded to, should personalize in future

      #- name: Cache CodeQL Results
      #  uses: actions/cache@v2
      #  with:
      #    path: ./results/python.sarif
      #    key: codeql
  upload:
    name: Upload sarif files
    runs-on: ubuntu-latest
    needs: [semgrep, codeql]
    steps:
      - name: Upload sarif files
        uses: actions/upload-artifact@v2
        with:
          name: aggregated-sarif
          path: path/to/artifact.txt
